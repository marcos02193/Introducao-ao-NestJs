services: 

  postgres:
    image: postgres
    container_name: postgresdb
    environment:
      POSTGRES_USER: marcosdb
      POSTGRES_PASSWORD: passworddb
      POSTGRES_DB: nestdb
    ports:
      - "5432:5432"

  api:
    build:
      context: .
      dockerfile: dockerfile.api
    container_name: nest-api
    env_file: .env
    ports:
      - 3000:3000
    depends_on:
      - postgres
    environment:
      DATABASE_URL: "${DATABASE_URL_CONTAINER}"
      CLOUDINARY_CLOUD_NAME: "dlixgq1nf"
      CLOUDINARY_API_KEY: "664385954891744"
      CLOUDINARY_API_SECRET: "g2NDHodUdcM23tAlvBXyPCfNfSk"
    command: sh -c "npx prisma migrate deploy && npm run start:prod"